package popo.atlas.app.page;

import popo.atlas.framework.utils.configurations.BrowserConfiguration;

public interface BasePage {

    BrowserConfiguration BROWSER_CONFIGURATION = BrowserConfiguration.getInstance();

}
